from __future__ import annotations


def extract_structured_with_gemini(raw_text: str) -> dict | None:
    """
    Structured extraction using Gemini multimodal model.
    Must return dict aligned with llama structured format.
    For now return None (stub).
    """
    _ = raw_text
    return None
