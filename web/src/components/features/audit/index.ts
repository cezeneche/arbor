/**
 * components/features/audit/
 * ────────────────────────────
 * Feature components for the /audit page.
 *
 * Planned exports:
 *   ChainValidityBanner   — AlertBanner success/error based on chain_valid
 *   AuditEventTimeline    — vertical timeline: extraction → compliance_pack
 *   EventDetailDrawer     — slide-in sheet with full event_json + HMAC
 *   AuditExportButton     — triggers S3 export, shows download link on success
 *
 * TODO: Implement in next build step.
 */
export {};
