/**
 * components/features/dashboard/
 * ─────────────────────────────────
 * Feature components for the /dashboard page.
 *
 * Planned exports:
 *   DashboardKPIs        — MetricCard grid (total CO₂e, cases, quality %)
 *   RecentCasesTable     — sortable table with StatusDot per row
 *   QuickActions         — Upload + Create Case CTA buttons
 *   ActionRequiredBanner — AlertBanner shown when cases need attention
 *
 * TODO: Implement in next build step.
 */
export {};
